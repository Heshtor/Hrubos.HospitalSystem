@model IList<ExaminationType>

@{
    ViewData["Title"] = "Typy vyšetření";
}
<h1>@ViewData["Title"]</h1>

<a asp-action="Create">Vytvořit nový typ vyšetření</a>

@if (Model != null && Model.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Název</th>
                <th scope="col">Smazat</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <th scope="row">@Model[i].Id</th>
                    <td>@Model[i].Name</td>
                    <td><a asp-action="Delete" asp-route-id="@Model[i].Id" onclick="return ConfirmDelete();">Smazat</a></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Tabulka typů vyšetření je prázdná!</p>
}

@section Scripts
{
    <script src="~/js/dialogs.js"></script>
}
