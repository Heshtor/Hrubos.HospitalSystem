@model Vaccination
@{
    ViewData["Title"] = "Objednat se na očkování";
}

@section Styles
{
    <link rel="stylesheet" href="~/css/form.min.css" asp-append-version="true" />
}

<section class="section-container">
    <div class="section-header">
        <h2>@ViewData["Title"]</h2>
    </div>

    <div class="card">
        <form asp-action="Create" method="post">
            <div class="form-group">
                <label asp-for="DateTime">Požadovaný termín</label>
                <input asp-for="DateTime"
                       type="datetime-local"
                       asp-format="{0:yyyy-MM-ddTHH:mm}"
                       min="@DateTime.Now.AddDays(2).Date.ToString("yyyy-MM-ddTHH:mm")"
                       value="@(Model.DateTime == DateTime.MinValue ? DateTime.Now.AddDays(2).ToString("yyyy-MM-ddTHH:mm") : Model.DateTime.ToString("yyyy-MM-ddTHH:mm"))" />

                <span asp-validation-for="DateTime" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="VaccineTypeId">Typ vakcíny</label>
                <select asp-for="VaccineTypeId" asp-items="ViewBag.VaccineTypesList"></select>
                <span asp-validation-for="VaccineTypeId" class="text-danger"></span>
            </div>
            
            <div class="buttons">
                <button type="submit">
                    <i class='bx bx-injection'></i> Objednat se
                </button>
                <a asp-action="Select" class="button">
                    <i class='bx bx-x-circle'></i> Zrušit
                </a>
            </div>
        </form>
    </div>
</section>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
